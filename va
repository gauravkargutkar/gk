<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Be My Valentine 💕</title>

<style>
body {
    margin: 0;
    font-family: 'Arial', sans-serif;
    background: linear-gradient(135deg, #ffd6e8, #ffeef5);
    display: flex;
    justify-content: center;
    align-items: center;
    height: 100vh;
}

.card {
    background: white;
    border-radius: 20px;
    padding: 30px 20px;
    width: 90%;
    max-width: 360px;
    text-align: center;
    box-shadow: 0 10px 30px rgba(0,0,0,0.15);
}

.emoji {
    font-size: 40px;
    margin-bottom: 10px;
}

h1 {
    font-size: 22px;
    color: #333;
    margin-bottom: 25px;
}

button {
    border: none;
    border-radius: 14px;
    padding: 14px 24px;
    font-size: 18px;
    cursor: pointer;
}

#yesBtn {
    background: #ff4d79;
    color: white;
    width: 100%;
}

#noBtn {
    margin-top: 12px;
    background: #eee;
    color: #555;
    position: relative;
}

.footer {
    margin-top: 20px;
    font-size: 12px;
    color: #aaa;
}
</style>
</head>

<body>

<div class="card">
    <div class="emoji">🐻💕</div>

    <h1>Dhara Mundra,<br>will you be my Valentine?</h1>

    <button id="yesBtn" onclick="yesClicked()">Yes 💖</button>
    <br>
    <button id="noBtn" onmouseover="moveNo()">No 🙈</button>

    <div class="footer">Just say yes already 😄</div>
</div>

<script>
function yesClicked() {
    document.querySelector('.card').innerHTML = `
        <div class="emoji">💖🥰💖</div>
        <h1>Yayyy! 💕<br>Best Valentine Ever!</h1>
    `;
}

function moveNo() {
    const btn = document.getElementById("noBtn");
    const x = Math.random() * 200 - 100;
    const y = Math.random() * 200 - 100;
    btn.style.transform = `translate(${x}px, ${y}px)`;
}
</script>

</body>
</html>
